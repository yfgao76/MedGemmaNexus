# Skills

Skills bundle tools into reusable capability packs (e.g., prostate mpMRI, brain MRI).
The authoritative skill definitions live in `MRI_Agent/config/skills.json`; this folder contains
the lightweight registry loader and formatting helpers used by the planner/policy prompts.
