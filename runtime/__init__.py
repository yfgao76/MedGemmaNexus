"""
Runtime helper utilities shared across different agent runners (reactive loop, LangGraph, etc.).
The goal is to avoid duplicating critical logic (tool manifests, memory_digest, finalization).
"""

